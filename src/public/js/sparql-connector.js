// from spec
getTriplesForProperty(graphURI, propertyURI)

setEndpoint(URL)

addPropertyToClass(graphURI, class, property)

addClassToProperty(graphURI, property, class)

addTextValue(graphURI, resource, property, value, language)

//////
addTriple(graphURI, s, p, o)

historyAdd(before, after)

generateSnapshotID()

diff(graphURI, before, after)
